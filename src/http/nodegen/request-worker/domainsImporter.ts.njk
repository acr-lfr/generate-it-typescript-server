{% for key, value in groupNamesWithFirstUrlSegment -%}
{% set domainClassName = ucFirst(key) + 'Domain' -%}
import {{ domainClassName }} from '@/domains/{{ domainClassName }}';
{% endfor -%}

export {
  {% for key, value in groupNamesWithFirstUrlSegment -%}
  {% set domainClassName = ucFirst(key) + 'Domain' -%}
  {{ domainClassName }},
  {% endfor %}
};
